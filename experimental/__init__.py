"""Experimental, unsupported host-side helpers. See experimental/README.md.

Nothing in the supported evidence workflow imports this package at import time.
The only supported touch point is the optional semantic backend
(tools/hybrid_search.py), which is imported lazily and only when the semantic
extras are installed.
"""
